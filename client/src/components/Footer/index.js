import React from "react";

const Footer = () => {
      return (
        <footer className="githubLink">
            <a href="">GitHub</a>
        </footer>
      );
    };

    export default Footer;